Python 3.6 required 
Make sure that the bounding_boxes.txt is in the same folder as the script
First argument is the path to the dataset
Second is the save path.

exmpl.

python cropper.py data/SET_A data/cropped

required libraries

numpy
imageio
